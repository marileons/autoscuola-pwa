const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),root="C:/agenda_zena/Agenda-Istruttori-v1-21-LAVORO",read=file=>fs.readFileSync(path.join(root,file),"utf8");
test("Altre funzioni espone ESAMI e tutte le schermate richieste",()=>{const html=read("index.html");assert.match(html,/id="openExaminers"[\s\S]*?id="openExams"/);for(const id of ["exams","newExam","examForm","examDate","examStartTime","examEndTime","examLocation","addExamStudents","examParticipants","examStudentPicker","examLocations"])assert.match(html,new RegExp(`id="${id}"`))});
test("UI usa ID stabili, selezione multipla accessibile ed esiti esclusivi",()=>{const app=read("app.js");assert.match(app,/participant\.studentId/);assert.match(app,/type="radio" name="\$\{group\}"/);assert.match(app,/multi-student-option exam-student-option/);assert.match(app,/L’allievo resterà nell’archivio/);assert.match(app,/participant\.outcome=input\.value/);assert.doesNotMatch(app,/participant\.rejectionNote=""/)});
test("PDF include esami e casi completi senza duplicare la fonte",()=>{const report=read("r10-features.js"),model=read("exams.js");for(const value of ["studentExamsReportHtml","Motivazione:","ESAMI ("])assert.match(report,new RegExp(value.replace(/[()]/g,"\\$&")));assert.match(model,/Esito non inserito/);assert.match(report,/state\.exams/);assert.match(report,/overflow-wrap:anywhere/)});
test("backup formato 4 aggiunge campi opzionali senza toccare parser progressivo",()=>{const backup=read("full-backup.js"),stream=read("full-backup-stream.js");assert.match(backup,/FORMAT_VERSION=4/);assert.match(backup,/AgendaExamStore\.snapshot/);assert.match(backup,/AgendaExamStore\.replaceAll/);assert.match(backup,/appData\.exams\|\|\[\]/);assert.match(stream,/parseLegacyBackup/)});
test("caricamento protetto inserisce exams prima dell'app e attende IndexedDB",()=>{const auth=read("auth-client.js"),exam=auth.indexOf('"exams.js'),app=auth.indexOf('"app.js');assert.ok(exam>=0&&exam<app);assert.match(auth,/AgendaAppReady/)});

test("orari testuali mobile e fine facoltativa",()=>{const html=read("index.html"),css=read("style.css"),app=read("app.js");assert.match(html,/examStartTime" type="text" inputmode="decimal" autocomplete="off"/);assert.match(html,/Ora di fine \(facoltativa\)/);assert.match(css,/exam-form-view input[^}]*font-size:16px/);assert.match(app,/Ora fine non inserita/)});

test("elenco ESAMI mostra ultimo, espande e conserva lo stato",()=>{const html=read("index.html"),app=read("app.js");assert.match(html,/id="toggleExams"[^>]+aria-expanded="false"/);assert.match(html,/Nessun esame salvato\. Premi NUOVO ESAME/);assert.match(app,/function compareExamsNewestFirst/);assert.match(app,/String\(b\.date/);assert.match(app,/String\(b\.startTime/);assert.match(app,/updatedAt/);assert.match(app,/createdAt/);assert.match(app,/localeCompare\(String\(b\.id/);assert.match(app,/state\.examsExpanded\?sorted:sorted\.slice\(0,1\)/);assert.match(app,/sorted\.length<2/);assert.match(app,/MOSTRA SOLO L’ULTIMO/);assert.match(app,/VISUALIZZA TUTTI GLI ESAMI/);assert.match(app,/state\.examsExpanded=!state\.examsExpanded/);const back=app.match(/\$\("backExamForm"\)\.onclick=\(\)=>\{renderExams\(\);show\("exams"\)\}/);assert.ok(back,"INDIETRO deve renderizzare senza azzerare examsExpanded")});

test("ordinamento ESAMI è deterministico per data ora aggiornamento creazione e ID",()=>{const app=read("app.js"),source=app.match(/function compareExamsNewestFirst\(a,b\)\{[^\n]+\}/)?.[0];assert.ok(source);const compare=Function(`${source};return compareExamsNewestFirst`)(),items=[{id:"z",date:"2026-09-10",startTime:"09:00",createdAt:1,updatedAt:1},{id:"old",date:"2026-09-09",startTime:"18:00",createdAt:9,updatedAt:9},{id:"b",date:"2026-09-10",startTime:"09:00",createdAt:2,updatedAt:3},{id:"a",date:"2026-09-10",startTime:"09:00",createdAt:2,updatedAt:3},{id:"late",date:"2026-09-10",startTime:"10:00",createdAt:1,updatedAt:1}];assert.deepEqual(items.sort(compare).map(x=>x.id),["late","a","b","z","old"])});
test("zero uno e più esami hanno controlli coerenti",()=>{const app=read("app.js");assert.match(app,/sorted\.length>0/);assert.match(app,/sorted\.length<2/);assert.match(app,/sorted\.slice\(0,1\)/);assert.match(app,/for\(const exam of visible\)/)});
